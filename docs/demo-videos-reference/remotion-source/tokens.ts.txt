// GATE 1 décision 1 — tokens dédiés aux vidéos démo automatisations NBHC.
// Indépendant de components/motions/design-tokens.ts (pipeline produit Vlogyz)
// et de marketing/tokens.ts (palette "cosmic dark" des pubs Vlogyz) : ces
// vidéos vivent sur nbhc.fr, leur cohérence visuelle doit être avec le site
// NBHC, pas avec Vlogyz. Ne modifie aucun des deux fichiers existants.

// Palette "Apple" — fond quasi noir neutre (pas de dominante bleu-violet),
// texte blanc cassé / gris Apple, accent bleu système (#0A84FF, systemBlue
// dark mode). Remplace la palette indigo/glow #5B5BD6 initiale — cf demande
// Nhia "vrai style Apple" + repère HIG (developer.apple.com/design/
// human-interface-guidelines/dark-mode : systemBlue dark = #0A84FF).
//
// Anti "AI slop" (recherche X/web du 2026-07-22, cf discours désign taste/
// unslop-ui) : le violet/indigo et le glassmorphism (blur flouté) sont les
// deux tells les plus cités d'un rendu "généré par IA". Violet déjà retiré.
// glassDark passe ici de 0.78 à 0.94 d'opacité (quasi solide) et les glows
// couleur sont réduits (0.40→0.20) — les cartes utilisent maintenant une
// surface unie + bordure fine, sans blur ni halo néon, dans chaque composant.
export const COLORS = {
  bg: "#000000",
  bgElevated: "#0A0A0C",
  glassDark: "rgba(26, 26, 28, 0.94)",
  glassDarkBorder: "rgba(255, 255, 255, 0.12)",
  text: "#F5F5F7",
  textMuted: "#86868B",
  textDim: "#6E6E73",
  accent: "#0A84FF",
  accentSoft: "rgba(10, 132, 255, 0.14)",
  accentGlow: "rgba(10, 132, 255, 0.20)",
  accentBorder: "rgba(10, 132, 255, 0.32)",
  positive: "#30D158",
  positiveSoft: "rgba(48, 209, 88, 0.14)",
  positiveGlow: "rgba(48, 209, 88, 0.20)",
  negative: "#FF453A",
  negativeSoft: "rgba(255, 69, 58, 0.14)",
  pure: "#FFFFFF",
} as const;

export const FONTS = {
  display: "Plus Jakarta Sans",
  body: "Manrope",
} as const;

// Polish "Apple" — ressorts moins rebondissants, réglage quasi-critique
// (damping haut / stiffness bas) : entrée douce, aucun overshoot marqué.
// Remplace les valeurs précédentes (plus "pop") pour tous les composants
// nbhc/* qui importent SPRINGS d'ici — changement global intentionnel.
export const SPRINGS = {
  enter: { mass: 0.9, damping: 26, stiffness: 90 },
  exit: { mass: 0.8, damping: 24, stiffness: 100 },
  gentle: { mass: 1, damping: 22, stiffness: 70 },
} as const;

// Format unique 16:9 — pas de multi-aspect ici (contrairement au pipeline
// produit), donc pas besoin d'adaptiveFontSize : tailles en px fixes dans
// chaque composant, pensées pour 1920x1080.
export const FORMAT = {
  width: 1920,
  height: 1080,
  fps: 30,
  durationFrames: 450, // 15s par défaut — cible brief 12-18s, ajustable par vidéo.
} as const;
